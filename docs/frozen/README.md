# FROZEN — do not read for instructions

Frozen 2026-09-21 by the owner. The `v2/` spec set (roadmap, gates, rollback plans,
quarantine policy, implementation-status) is **not** the current plan. It was
replaced by [docs/plan/README.md](../plan/README.md).

Why frozen: the documentation had grown larger than the product. The owner chose
speed and product quality over gated delivery. Nothing here is an instruction,
a requirement, or a gate.

Read a file here only when a current plan file explicitly links to it (e.g. the
interaction spec's acceptance cases as *reference*, or ADRs in
`v2/decision-records/` for geometry conventions). Reverify anything against code.
