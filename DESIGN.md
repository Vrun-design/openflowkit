# Design reference

Start at [docs/README.md](docs/README.md).

The full v2 redesign brief lives in [design system](docs/v2/design-system.md).
Behavior lives in [interaction specification](docs/v2/interaction-spec.md).
Existing code tokens/components are audit evidence only; v2 reuse must pass the new
system's gates. This file is only a pointer.
