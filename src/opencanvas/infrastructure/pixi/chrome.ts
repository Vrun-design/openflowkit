// Canvas chrome (selection frames, handles, marquee, connector editing) shares
// one accent so it reads as "the tool", never as content. Brand orange stays
// in the UI shell.
export const CHROME_ACCENT = 0x3b82f6;
export const CHROME_ACCENT_DEEP = 0x1d4ed8;
export const CHROME_SURFACE = 0xffffff;
