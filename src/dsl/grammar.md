# OpenFlow DSL v1

Canonical language specification: [`docs/plan/grammar.md`](../../docs/plan/grammar.md).

This pointer intentionally prevents two grammar copies from drifting. Parser, compiler,
serializer, fixtures, and round-trip laws in this directory implement that specification.
